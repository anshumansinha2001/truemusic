/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "res.cloudinary.com",
      "anshumansinha2001.github.io",
    ],
  },
};

export default nextConfig;
